  現在の状態まとめ:

  ✅ 実装完了:
  - メール認証機能（SendGrid経由）
  - HTTPリダイレクト → Deep Link の仕組み
  - パスワードリセット機能
  - アプリ名を「映単語」に変更

  ⚠️ 開発中の制限:
  - Expo Goではカスタムスキームが動作しない
  - メール認証リンクは機能しない（本番環境では動作）

  ✅ 本番環境で動作させる手順（将来）:
  1. バックエンドを公開サーバーにデプロイ
  2. .envのSITE_URLを本番URLに変更
  3. アプリをビルド（EAS Build）
  4. Deep Linkが自動的に動作 ✅